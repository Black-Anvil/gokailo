{% comment %} 
How to add installed app in admin panel to the queue:

1. Find url of this app inside page source
2. Get uniq string inside this url: 

example url: "https:\/\/js.klevu.com\/klevu-js-v1\/js\/klevuScript.js", 
then uniq string could be: klevuScript.js

3. Add prefix "__app_" to uniq string
4. Attach this "__app_klevuScript.js" string to the one of arrays
{% endcomment %}

<script>
__engine_objects_queue['loaded'] = ['loaded'];
__engine_objects_queue['scriptsloading'] = ['scriptsloading'];
__engine_objects_queue['scriptsloaded'] = ['scriptsloaded', 'shogun','__app_cart.js'];
__engine_objects_queue['other'] = ['other'];
__engine_objects_queue['event'] = ['footer','gempagev2'];

{%- if template.name == 'product' -%}
__engine_objects_queue['event'].push('bold-common')	
{%- endif -%}
</script>