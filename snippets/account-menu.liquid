<style>
    .account-menu {
        padding: 12px 0;
        border-radius: 6px;
        color: var(--grays3);
        background-color: var(--grays6);
    }

    .account-menu a {
        position: relative;
        display: block;
        padding: 8px 24px;
    }

    .account-menu a.active {
        border-left: 2px solid var(--primary2);
        color: var(--primary2);
        text-decoration: none;
        pointer-events: none;
    }
</style>

<ul class="account-menu">
    <li>
        <a href="{{ routes.account_url }}" class="h6-title {% if active == "account" %}{{ "active" }}{% endif %}">Orders History</a>
    </li>
    <li>
        <a href="{{ routes.account_addresses_url }}" class="h6-title {% if active == "addresses" %}{{ "active" }}{% endif %}">Addresses</a>
    </li>
    <li>
        <a href="{{ routes.account_logout_url }}" class="h6-title">Log out</a>
    </li>
</ul>
