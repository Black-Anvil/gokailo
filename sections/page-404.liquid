<style>
    .error {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
    }

    .error__title {
        position: relative;
        margin: 0 0 16px;
        padding: 0 0 16px;
        color: var(--primary5);
        text-align: center;
        font-size: 100px;
        font-weight: 700;
        line-height: 100%;
        text-transform: uppercase;
    }

    .error__title::after {
        content: "";
        position: absolute;
        bottom: 0;
        left: 50%;
        width: 120px;
        height: 1px;
        background-color: var(--primary1);
        transform: translateX(-50%);
    }

    .error__text {
        margin: 0 0 24px;
    }

    @media screen and (max-width: 768px) {
        .error__title {
            font-size: 80px;
        }
    }
</style>

<div class="page-inner">
    <div class="container">
        <div class="error">
            <p class="error__title">404</p>
            <p class="error__text h5-title">Sorry, the requested page not found</p>
            <a class="button button--primary" href="{{ routes.root_url }}">back to homepage</a>
        </div>
    </div>
</div>
